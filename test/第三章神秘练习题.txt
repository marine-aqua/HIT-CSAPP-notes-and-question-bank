在 x86-64 架构的 CPU 抽象模型中，指令指针寄存器（RIP）的主要功能是（ ）。
A. 记录最近一次算术或逻辑运算的条件状态（如进位、溢出等）
B. 作为通用数据寄存器暂存指令的源/目的操作数
C. 维护当前函数调用栈的栈顶与栈基址指针位置
D. 保存 CPU 下一条待取指指令的内存线性地址

在 x86-64 架构中，执行指令 movl $0x12345678, %eax 后，64 位通用寄存器 %rax 的高 32 位将发生何种变化？（ ）
A. 根据立即数的符号位进行符号扩展
B. 保持执行前的原有数值不变
C. 全部置为 1（即填充 0xFFFFFFFF）
D. 全部自动清零

在 x86-64 架构（AT&T 语法）下，执行指令 leaq 8(%rdi, %rsi, 2), %rax。若执行前寄存器 %rdi 与 %rsi 的值分别为 0x1000 和 0x100，则指令执行后 %rax 中的值为（ ）。
A. 0x1008
B. 0x2008
C. 0x1208
D. 触发内存读取操作，将地址 0x1208 处的数据载入 %rax

在 x86-64 汇编语言中，movq 指令不允许采用的操作数组合是（ ）。
A. 源操作数与目的操作数均为内存单元
B. 源操作数为通用寄存器，目的操作数为内存单元
C. 源操作数为立即数，目的操作数为通用寄存器
D. 源操作数为立即数，目的操作数为内存单元

在 x86-64 架构中，执行 pushq %rbp 指令时，硬件层面实际完成的操作序列为（ ）。
A. 仅将 %rbp 的内容拷贝至 %rsp 寄存器
B. R[%rsp] 加 8，随后将 %rbp 的值写入 M[%rsp]
C. R[%rsp] 减 8，随后将 %rbp 的值写入 M[%rsp]
D. 将 %rbp 的值写入 M[%rsp]，随后 R[%rsp] 加 8

在 x86-64 架构下，编译器常利用移位与地址计算指令对常数乘法进行强度削弱（Strength Reduction）。若寄存器 %rdi 中存放变量 x，下列指令序列中符合该优化策略且能正确计算 x * 12 的是（ ）。
A. shlq $12, %rdi
B. leaq (%rdi, %rdi, 11), %rax
C. imulq $12, %rdi, %rax
D. leaq (%rdi, %rdi, 2), %rax 后接 shlq $2, %rax

在 x86-64 架构中，执行单操作数有符号除法指令 idivq %rcx 时，被除数隐含存放在（ ）。
A. 由显式操作数寄存器 %rcx 直接提供
B. 仅 %rax 寄存器中
C. 仅 %rdi 寄存器中
D. 由 %rdx 与 %rax 组合构成的 128 位数据中

在 x86-64 汇编语言中，指令 testq %rdi, %rdi 常用于判断寄存器 %rdi 的值是否为零。该指令执行后，主要依据哪个状态标志位的状态进行后续条件分支判断？（ ）
A. OF
B. ZF
C. CF
D. SF

在 x86-64 汇编语言中，条件传送指令 cmovge %rdx, %rax（ge 表示有符号数大于或等于）执行数据传送的前提条件是下列哪个标志位组合成立？（ ）
A. ZF = 1
B. SF = 1 且 ZF = 0
C. CF = 0
D. SF = OF

在 x86-64 架构（AT&T 语法）下，已知寄存器 %rdx 与 %rcx 的值分别为 0xF000 和 0x0100，则寻址表达式 0x80(,%rdx,2) 计算出的有效内存地址为（ ）。
A. 0x0F180
B. 0x1E000
C. 0x10080
D. 0x1E080

在 x86-64 架构（AT&T 语法）中，若需将完整的 64 位立即数载入寄存器，通常需使用 movabsq 而非标准 movq 指令。其根本原因在于（ ）。
A. movabsq 指令的微架构执行延迟更低
B. 标准 movq 的立即数字段仅支持 32 位编码，且运算结果会自动符号扩展至 64 位
C. 标准 movq 指令不支持向通用寄存器直接写入数据
D. 目的寄存器 %rax 的物理位宽上限为 32 位

在 x86-64 汇编语言中，算术右移指令 sarq 与逻辑右移指令 shrq 的核心区别在于（ ）。
选项：
A. 操作数位宽支持不同，sarq 仅限 8 位数据，shrq 支持 64 位数据
B. 状态标志位更新机制不同，sarq 不修改条件码，shrq 会更新条件码
C. 移位后高位填充规则不同，sarq 填充 0，shrq 填充原符号位
D. 移位后高位填充规则不同，sarq 填充原符号位，shrq 填充 0